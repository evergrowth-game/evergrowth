Binary files automobiles_pck_remote/automobiles_beetle/textures/automobiles_beetle.png and automobiles_pck/automobiles_beetle/textures/automobiles_beetle.png differ
Binary files automobiles_pck_remote/automobiles_beetle/textures/beetle_body.png and automobiles_pck/automobiles_beetle/textures/beetle_body.png differ
Binary files automobiles_pck_remote/automobiles_buggy/textures/automobiles_buggy.png and automobiles_pck/automobiles_buggy/textures/automobiles_buggy.png differ
Binary files automobiles_pck_remote/automobiles_catrelle/textures/automobiles_catrelle.png and automobiles_pck/automobiles_catrelle/textures/automobiles_catrelle.png differ
Binary files automobiles_pck_remote/automobiles_coupe/textures/automobiles_coupe.png and automobiles_pck/automobiles_coupe/textures/automobiles_coupe.png differ
diff -x .git -ur automobiles_pck_remote/automobiles_delorean/control.lua automobiles_pck/automobiles_delorean/control.lua
--- automobiles_pck_remote/automobiles_delorean/control.lua	2026-03-02 06:48:07
+++ automobiles_pck/automobiles_delorean/control.lua	2026-02-04 11:12:20
@@ -14,7 +14,10 @@
 		
         local acc = 0
         if self._energy > 0 then
-            if longit_speed < max_speed and ctrl.up then
+            local speed_factor = automobiles_lib.get_speed_factor(self.object)
+            local effective_max_speed = max_speed * speed_factor
+
+            if longit_speed < effective_max_speed and ctrl.up then
                 --get acceleration factor
                 acc = automobiles_lib.check_road_is_ok(self.object, max_acc_factor)
                 --minetest.chat_send_all('engineacc: '.. engineacc)
diff -x .git -ur automobiles_pck_remote/automobiles_delorean/flight.lua automobiles_pck/automobiles_delorean/flight.lua
--- automobiles_pck_remote/automobiles_delorean/flight.lua	2026-03-02 06:48:07
+++ automobiles_pck/automobiles_delorean/flight.lua	2026-01-31 12:12:17
@@ -65,7 +65,7 @@
 function delorean.turn_flight_mode(self)
     if self._is_flying == 1 then
         --initial lift
-        self._car_gravity = 10
+        self._car_gravity = 5
         local curr_pos = self.object:get_pos()
         curr_pos.y = curr_pos.y + 1.5
         self.object:move_to(curr_pos)
Binary files automobiles_pck_remote/automobiles_delorean/textures/automobiles_delorean.png and automobiles_pck/automobiles_delorean/textures/automobiles_delorean.png differ
diff -x .git -ur automobiles_pck_remote/automobiles_lib/control.lua automobiles_pck/automobiles_lib/control.lua
--- automobiles_pck_remote/automobiles_lib/control.lua	2026-03-02 06:48:07
+++ automobiles_pck/automobiles_lib/control.lua	2026-02-04 11:12:07
@@ -2,11 +2,22 @@
 
 automobiles_lib.vector_up = vector.new(0, 1, 0)
 
+function automobiles_lib.get_speed_factor(obj)
+	local pos_below = obj:get_pos()
+	if not pos_below then return 1.0 end
+	pos_below.y = pos_below.y - 0.1
+	local node_below = core.get_node(pos_below).name
+	if node_below:find("autobahn:node") then
+		return 1.5
+	end
+	return 1.0
+end
+
 function automobiles_lib.check_road_is_ok(obj, max_acc_factor)
 	local pos_below = obj:get_pos()
 	pos_below.y = pos_below.y - 0.1
 	local node_below = core.get_node(pos_below).name
-    --core.chat_send_all(node_below)
+	--core.chat_send_all(node_below)
     local nodedef = core.registered_nodes[node_below]
     if nodedef.liquidtype == "none" then
         local slow_nodes = {
@@ -14,6 +25,7 @@
                             ['default:cave_ice'] = 0.01,
                            }
         local acc = slow_nodes[node_below]
+        -- Note: Autobahn speed boost is now handled via get_speed_factor affecting max_speed, not acceleration
         if acc == nil then acc = max_acc_factor end
         return acc
     else
@@ -98,7 +110,10 @@
                     end
                 end
             else
-                if longit_speed < max_speed and ctrl.up then
+                local speed_factor = automobiles_lib.get_speed_factor(self.object)
+                local effective_max_speed = max_speed * speed_factor
+
+                if longit_speed < effective_max_speed and ctrl.up then
                     --get acceleration factor
                     acc = automobiles_lib.check_road_is_ok(self.object, max_acc_factor)
                     --core.chat_send_all('engineacc: '.. engineacc)
@@ -208,14 +223,19 @@
 		        self._steering_angle = math.min(self._steering_angle+steering_speed*dtime,steering_limit)
             else
                 --center steering
-                if longit_speed > 0 then
-                    local factor = 1
-                    if self._steering_angle > 0 then factor = -1 end
-                    local correction = (steering_limit*(longit_speed/75)) * factor
-                    local before_correction = self._steering_angle
-                    self._steering_angle = self._steering_angle + correction
-                    if math.sign(before_correction) ~= math.sign(self._steering_angle) then self._steering_angle = 0 end
-                end
+                --center steering
+                local abs_speed = math.abs(longit_speed)
+                 local factor = 1
+                if self._steering_angle > 0 then factor = -1 end
+                
+                -- smooth centering dependent on time, not frames
+                -- centering speed increases with car speed, but has a minimum base value
+                local centering_speed = (steering_limit * (abs_speed / 15)) + 20
+                local correction = centering_speed * dtime * factor
+                
+                local before_correction = self._steering_angle
+                self._steering_angle = self._steering_angle + correction
+                if math.sign(before_correction) ~= math.sign(self._steering_angle) then self._steering_angle = 0 end
 	        end
         end
 
diff -x .git -ur automobiles_pck_remote/automobiles_lib/entities.lua automobiles_pck/automobiles_lib/entities.lua
--- automobiles_pck_remote/automobiles_lib/entities.lua	2026-03-02 06:48:07
+++ automobiles_pck/automobiles_lib/entities.lua	2026-02-25 22:07:17
@@ -273,83 +273,63 @@
     local pos = self.object:get_pos()
 
     local front_suspension=minetest.add_entity(self.object:get_pos(),self._front_suspension_ent)
-    if front_suspension then
-        front_suspension:set_attach(self.object,'',self._front_suspension_pos,{x=0,y=0,z=0})
-        self.front_suspension = front_suspension
-    end
+    front_suspension:set_attach(self.object,'',self._front_suspension_pos,{x=0,y=0,z=0})
+    self.front_suspension = front_suspension
 
     if self._front_wheel_ent then
         local lf_wheel=minetest.add_entity(pos,self._front_wheel_ent)
-        if lf_wheel then
-            lf_wheel:set_attach(self.front_suspension,'',{x=-self._front_wheel_xpos,y=0,z=0},{x=0,y=0,z=0})
-	        -- set the animation once and later only change the speed
-            lf_wheel:set_animation(self._front_wheel_frames, 0, 0, true)
-            self.lf_wheel = lf_wheel
-        end
+        lf_wheel:set_attach(self.front_suspension,'',{x=-self._front_wheel_xpos,y=0,z=0},{x=0,y=0,z=0})
+	    -- set the animation once and later only change the speed
+        lf_wheel:set_animation(self._front_wheel_frames, 0, 0, true)
+        self.lf_wheel = lf_wheel
 
         local rf_wheel=minetest.add_entity(pos,self._front_wheel_ent)
-        if rf_wheel then
-            rf_wheel:set_attach(self.front_suspension,'',{x=self._front_wheel_xpos,y=0,z=0},{x=0,y=180,z=0})
-	        -- set the animation once and later only change the speed
-            rf_wheel:set_animation(self._front_wheel_frames, 0, 0, true)
-            self.rf_wheel = rf_wheel
-        end
+        rf_wheel:set_attach(self.front_suspension,'',{x=self._front_wheel_xpos,y=0,z=0},{x=0,y=180,z=0})
+	    -- set the animation once and later only change the speed
+        rf_wheel:set_animation(self._front_wheel_frames, 0, 0, true)
+        self.rf_wheel = rf_wheel
     end
 
     local rear_suspension=minetest.add_entity(self.object:get_pos(),self._rear_suspension_ent)
-    if rear_suspension then
-        rear_suspension:set_attach(self.object,'',self._rear_suspension_pos,{x=0,y=0,z=0})
-        self.rear_suspension = rear_suspension
-    end
+    rear_suspension:set_attach(self.object,'',self._rear_suspension_pos,{x=0,y=0,z=0})
+    self.rear_suspension = rear_suspension
 
     if self._rear_wheel_ent then
         local lr_wheel=minetest.add_entity(pos,self._rear_wheel_ent)
-        if lr_wheel then
-            lr_wheel:set_attach(self.rear_suspension,'',{x=-self._rear_wheel_xpos,y=0,z=0},{x=0,y=0,z=0})
-	        -- set the animation once and later only change the speed
-            lr_wheel:set_animation(self._rear_wheel_frames, 0, 0, true)
-            self.lr_wheel = lr_wheel
-        end
+        lr_wheel:set_attach(self.rear_suspension,'',{x=-self._rear_wheel_xpos,y=0,z=0},{x=0,y=0,z=0})
+	    -- set the animation once and later only change the speed
+        lr_wheel:set_animation(self._rear_wheel_frames, 0, 0, true)
+        self.lr_wheel = lr_wheel
 
         local rr_wheel=minetest.add_entity(pos,self._rear_wheel_ent)
-        if rr_wheel then
-            rr_wheel:set_attach(self.rear_suspension,'',{x=self._rear_wheel_xpos,y=0,z=0},{x=0,y=180,z=0})
-	        -- set the animation once and later only change the speed
-            rr_wheel:set_animation(self._rear_wheel_frames, 0, 0, true)
-            self.rr_wheel = rr_wheel
-        end
+        rr_wheel:set_attach(self.rear_suspension,'',{x=self._rear_wheel_xpos,y=0,z=0},{x=0,y=180,z=0})
+	    -- set the animation once and later only change the speed
+        rr_wheel:set_animation(self._rear_wheel_frames, 0, 0, true)
+        self.rr_wheel = rr_wheel
     end
 
 
     if self._steering_ent then
 	    local steering_axis=minetest.add_entity(pos,'automobiles_lib:pivot_mesh')
-        if steering_axis then
-            steering_axis:set_attach(self.object,'',self._drive_wheel_pos,{x=self._drive_wheel_angle,y=0,z=0})
-            if steering_axis then
-	            self.steering_axis = steering_axis
+        steering_axis:set_attach(self.object,'',self._drive_wheel_pos,{x=self._drive_wheel_angle,y=0,z=0})
+	    self.steering_axis = steering_axis
 
-	            local steering=minetest.add_entity(self.steering_axis:get_pos(), self._steering_ent)
-                steering:set_attach(self.steering_axis,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
-	            self.steering = steering
-            end
-        end
+	    local steering=minetest.add_entity(self.steering_axis:get_pos(), self._steering_ent)
+        steering:set_attach(self.steering_axis,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
+	    self.steering = steering
     end
 
     if self._rag_retracted_ent then
         local rag_rect=minetest.add_entity(self.object:get_pos(),self._rag_retracted_ent)
-        if rag_rect then
-	        rag_rect:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
-	        self.rag_rect = rag_rect
-            self.rag_rect:set_properties({is_visible=false})
-        end
+	    rag_rect:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
+	    self.rag_rect = rag_rect
+        self.rag_rect:set_properties({is_visible=false})
     end
 
     if self._rag_extended_ent then
         local rag=minetest.add_entity(self.object:get_pos(),self._rag_extended_ent)
-        if rag then
-	        rag:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
-	        self.rag = rag
-        end
+	    rag:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
+	    self.rag = rag
     end
 
     automobiles_lib.seats_create(self)
@@ -358,55 +338,43 @@
     if self._gauge_pointer_ent then pointer_entity = self._gauge_pointer_ent end
     if self._fuel_gauge_pos then
         local fuel_gauge=minetest.add_entity(pos, pointer_entity)
-        if fuel_gauge then
-            fuel_gauge:set_attach(self.object,'',self._fuel_gauge_pos,{x=0,y=0,z=0})
-            self.fuel_gauge = fuel_gauge
-        end
+        fuel_gauge:set_attach(self.object,'',self._fuel_gauge_pos,{x=0,y=0,z=0})
+        self.fuel_gauge = fuel_gauge
     end
 
     if self._front_lights then
         local lights = minetest.add_entity(pos,self._front_lights)
-        if lights then
-            lights:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
-            self.lights = lights
-            self.lights:set_properties({is_visible=true})
-        end
+        lights:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
+        self.lights = lights
+        self.lights:set_properties({is_visible=true})
     end
 
     if self._rear_lights then
         local r_lights = minetest.add_entity(pos,self._rear_lights)
-        if r_lights then
-            r_lights:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
-            self.r_lights = r_lights
-            self.r_lights:set_properties({is_visible=true})
-        end
+        r_lights:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
+        self.r_lights = r_lights
+        self.r_lights:set_properties({is_visible=true})
     end
 
     if self._reverse_lights then
         local reverse_lights = minetest.add_entity(pos,self._reverse_lights)
-        if reverse_lights then
-            reverse_lights:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
-            self.reverse_lights = reverse_lights
-            self.reverse_lights:set_properties({is_visible=true})
-        end
+        reverse_lights:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
+        self.reverse_lights = reverse_lights
+        self.reverse_lights:set_properties({is_visible=true})
     end
 
     if self._turn_left_lights then
         local turn_l_light = minetest.add_entity(pos,self._turn_left_lights)
-        if turn_l_light then
-            turn_l_light:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
-            self.turn_l_light = turn_l_light
-            self.turn_l_light:set_properties({is_visible=true})
-        end
+        turn_l_light:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
+        self.turn_l_light = turn_l_light
+        self.turn_l_light:set_properties({is_visible=true})
     end
 
     if self._turn_right_lights then
         local turn_r_light = minetest.add_entity(pos,self._turn_right_lights)
-        if turn_r_light then
-            turn_r_light:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
-            self.turn_r_light = turn_r_light
-            self.turn_r_light:set_properties({is_visible=true})
-        end
+        turn_r_light:set_attach(self.object,'',{x=0,y=0,z=0},{x=0,y=0,z=0})
+        self.turn_r_light = turn_r_light
+        self.turn_r_light:set_properties({is_visible=true})
     end
 
     if self._extra_items_function then
@@ -457,21 +425,22 @@
     
 	local later_speed = automobiles_lib.dot(velocity,nhdir)
     local dynamic_later_drag = self._LATER_DRAG_FACTOR
-    if longit_speed > 2 then dynamic_later_drag = 2.0 end
-    if longit_speed > 8 then dynamic_later_drag = 0.5 end
-    --core.chat_send_all(dump(longit_speed))
+    -- if longit_speed > 2 then dynamic_later_drag = 2.0 end
+    -- if longit_speed > 8 then dynamic_later_drag = 0.5 end
 
     if automobiles_lib.extra_drift and longit_speed > (4*self._vehicle_power_scale) then
         dynamic_later_drag = dynamic_later_drag/(longit_speed*2)
     end
 
+    local later_drag_mag = (later_speed * later_speed) + (math.abs(later_speed) * 4)
+
     local later_drag = vector.new()
     if self._is_motorcycle == true then
-        later_drag = vector.multiply(nhdir,later_speed*
-            later_speed*self._LATER_DRAG_FACTOR*-1*automobiles_lib.sign(later_speed))
+        later_drag = vector.multiply(nhdir,later_drag_mag*
+            self._LATER_DRAG_FACTOR*-1*automobiles_lib.sign(later_speed))
     else
-        later_drag = vector.multiply(nhdir,later_speed*
-            later_speed*dynamic_later_drag*-1*automobiles_lib.sign(later_speed))
+        later_drag = vector.multiply(nhdir,later_drag_mag*
+            dynamic_later_drag*-1*automobiles_lib.sign(later_speed))
     end
 
     local accel = vector.add(longit_drag,later_drag)
@@ -586,7 +555,8 @@
 
         --control
         local steering_angle_max = 40
-        local steering_speed = 40
+        -- increased from 40 to 90 to improve keyboard response
+        local steering_speed = 90
         if math.abs(longit_speed) > 3*self._vehicle_scale then
             local mid_speed = (steering_speed/2)
             steering_speed = (mid_speed + (mid_speed / math.abs(longit_speed*0.25))*self._vehicle_scale)
@@ -841,8 +811,8 @@
 
 
             else
-                if self.front_suspension then self.front_suspension:set_rotation({x=0,y=0,z=0}) end
-                if self.rear_suspension then self.rear_suspension:set_rotation({x=0,y=0,z=0}) end
+                self.front_suspension:set_rotation({x=0,y=0,z=0})
+                self.rear_suspension:set_rotation({x=0,y=0,z=0})
             end
         end
     end
diff -x .git -ur automobiles_pck_remote/automobiles_lib/init.lua automobiles_pck/automobiles_lib/init.lua
--- automobiles_pck_remote/automobiles_lib/init.lua	2026-03-02 06:48:07
+++ automobiles_pck/automobiles_lib/init.lua	2026-02-03 11:20:23
@@ -23,7 +23,7 @@
 
 automobiles_lib.fuel = {['biofuel:biofuel'] = 1,['biofuel:bottle_fuel'] = 1,
                 ['biofuel:phial_fuel'] = 0.25, ['biofuel:fuel_can'] = 10,
-                ['airutils:biofuel'] = 1,}
+                ['airutils:biofuel'] = 1, ['techage:ta3_barrel_gasoline'] = 10, ['techage:ta3_canister_gasoline'] = 1}
 
 automobiles_lib.gravity = 9.8
 automobiles_lib.ideal_step = 0.2
@@ -178,12 +178,10 @@
             local max_seats = table.getn(self._seat_pos)
             for i=1, max_seats do
                 self._passengers_base[i] = minetest.add_entity(pos,'automobiles_lib:pivot_mesh')
-                if self._passengers_base[i] then
-                    if not self._seats_rot then
-                        self._passengers_base[i]:set_attach(self.object,'',self._seat_pos[i],{x=0,y=0,z=0})
-                    else
-                        self._passengers_base[i]:set_attach(self.object,'',self._seat_pos[i],{x=0,y=self._seats_rot[i],z=0})
-                    end
+                if not self._seats_rot then
+                    self._passengers_base[i]:set_attach(self.object,'',self._seat_pos[i],{x=0,y=0,z=0})
+                else
+                    self._passengers_base[i]:set_attach(self.object,'',self._seat_pos[i],{x=0,y=self._seats_rot[i],z=0})
                 end
             end
 
@@ -203,7 +201,6 @@
 function automobiles_lib.attach_driver(self, player)
     local name = player:get_player_name()
     self.driver_name = name
-    if not self.driver_seat then return end
 
     -- attach the driver
     player:set_attach(self.driver_seat, "", {x = 0, y = 0, z = 0}, {x = 0, y = 0, z = 0})
@@ -881,21 +878,19 @@
     minetest.register_on_joinplayer(function(player)
 	    local inv = player:get_inventory()
         local car = "automobiles_beetle:beetle"
-        if core.get_modpath("automobiles_beetle") and not inv:contains_item("main", car) then inv:add_item("main", car) end
+        if not inv:contains_item("main", car) then inv:add_item("main", car) end
         car = "automobiles_buggy:buggy"
-        if core.get_modpath("automobiles_buggy") and not inv:contains_item("main", car) then inv:add_item("main", car) end
+        if not inv:contains_item("main", car) then inv:add_item("main", car) end
         car = "automobiles_catrelle:catrelle_4f"
-        if core.get_modpath("automobiles_catrelle") and not inv:contains_item("main", car) then inv:add_item("main", car) end
+        if not inv:contains_item("main", car) then inv:add_item("main", car) end
         car = "automobiles_coupe:coupe"
-        if core.get_modpath("automobiles_coupe") and not inv:contains_item("main", car) then inv:add_item("main", car) end
+        if not inv:contains_item("main", car) then inv:add_item("main", car) end
         car = "automobiles_delorean:delorean"
-        if core.get_modpath("automobiles_delorean") and not inv:contains_item("main", car) then inv:add_item("main", car) end
+        if not inv:contains_item("main", car) then inv:add_item("main", car) end
         car = "automobiles_delorean:time_machine"
-        if core.get_modpath("automobiles_delorean") and not inv:contains_item("main", car) then inv:add_item("main", car) end
+        if not inv:contains_item("main", car) then inv:add_item("main", car) end
         car = "automobiles_trans_am:trans_am"
-        if core.get_modpath("automobiles_trans_am") and not inv:contains_item("main", car) then inv:add_item("main", car) end
-        car = "automobiles_beetle:beetle_conv"
-        if core.get_modpath("automobiles_beetle") and not inv:contains_item("main", car) then inv:add_item("main", car) end
+        if not inv:contains_item("main", car) then inv:add_item("main", car) end
     end)
 end
 
Binary files automobiles_pck_remote/automobiles_lib/textures/automobiles_engine.png and automobiles_pck/automobiles_lib/textures/automobiles_engine.png differ
Binary files automobiles_pck_remote/automobiles_lib/textures/automobiles_painter.png and automobiles_pck/automobiles_lib/textures/automobiles_painter.png differ
Binary files automobiles_pck_remote/automobiles_lib/textures/automobiles_wheel_icon.png and automobiles_pck/automobiles_lib/textures/automobiles_wheel_icon.png differ
Binary files automobiles_pck_remote/automobiles_motorcycle/textures/automobiles_motorcycle.png and automobiles_pck/automobiles_motorcycle/textures/automobiles_motorcycle.png differ
Binary files automobiles_pck_remote/automobiles_roadster/textures/automobiles_roadster.png and automobiles_pck/automobiles_roadster/textures/automobiles_roadster.png differ
Binary files automobiles_pck_remote/automobiles_trans_am/textures/automobiles_trans_am.png and automobiles_pck/automobiles_trans_am/textures/automobiles_trans_am.png differ
Binary files automobiles_pck_remote/automobiles_vespa/textures/automobiles_vespa.png and automobiles_pck/automobiles_vespa/textures/automobiles_vespa.png differ
